<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ site.name }}</title>
    <link rel="stylesheet" href="{{ site.url }}/wp-content/themes/{{ theme.name }}/style.css">
</head>
<body>
    <header>
        <nav>
            {{ wp_nav_menu('primary') }}
        </nav>
    </header>

    <main>
        {{ content }}
    </main>

    <footer>
        <p>&copy; {{ "now"|date("Y") }} {{ site.name }}</p>
    </footer>
</body>
</html>
